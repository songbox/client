<h1 class="song-title">{{song.title}}</h1>

{{#if song.format}}
  {{component (concat "song-sheet/" song.format "-sheet")
    sections=sections
    showChords=showChords
    showSectionHeaders=showSectionHeaders
    showMultiColumn=showMultiColumn
  }}
{{else}}
  No format specified
{{/if}}
